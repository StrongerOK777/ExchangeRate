[theme]
primaryColor = "#1a73e8"

backgroundColor = "#ffffff"

secondaryBackgroundColor = "#f0f2f6"

textColor = "#262730"

font = "sans serif"


[server]
# 文件变更时自动重载（本地开发用，云端无害）
runOnSave = true

[browser]
# 关闭向 Streamlit 官方发送匿名统计
gatherUsageStats = false
